import styled from "styled-components";
import { colors } from "@/theme/colors";

const PanelSelectedSpan = styled.span`
    color: ${colors.yellow};
    font-size: 1.3rem;
`;

export default PanelSelectedSpan;

